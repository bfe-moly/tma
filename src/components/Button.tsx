import {
  useColorScheme,
  useTheme,
  usePlatform,
  AppearanceProvider,
} from '@twa-dev/mark42';
import React from 'react';

export const MyButton = (props: any) => {
  return <button {...props} />;
};
